#pragma once
// No CDN, cloud service or external assets: served directly by the monitor.
const char WEB_UI[] PROGMEM = R"CO2HTML(<!doctype html>
<html lang="uk"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="#111a1a"><title>CO₂ Monitor</title>
<style>
:root{color-scheme:dark;--bg:#101717;--card:#192323;--line:#30413f;--text:#e6f0e8;--muted:#9cb0a8;--accent:#c3ef98;--warn:#ffc37a}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font:15px/1.5 system-ui,-apple-system,sans-serif}main{max-width:1100px;margin:auto;padding:26px 24px 70px}header{display:flex;align-items:center;justify-content:space-between;gap:16px;margin-bottom:32px}.brand{font-size:20px;font-weight:750;letter-spacing:-.5px}.brand small{color:var(--muted);font-size:12px;margin-left:10px;font-weight:400}.status{font-size:12px;color:var(--accent)}nav{display:flex;gap:8px;overflow:auto;padding-bottom:14px;margin-bottom:12px;border-bottom:1px solid var(--line)}button,.button{font:inherit;cursor:pointer;border:1px solid var(--line);background:transparent;color:var(--text);border-radius:10px;padding:10px 15px;text-decoration:none;display:inline-block}button:hover,.button:hover{border-color:var(--accent)}button:disabled{opacity:.4;cursor:wait}nav button{white-space:nowrap;border-color:transparent;color:var(--muted)}nav button[aria-selected=true],.primary{background:var(--accent);color:#172210;border-color:var(--accent)}h1{font-size:30px;line-height:1.15;margin:26px 0 8px;letter-spacing:-1px}h2{font-size:17px;margin:0 0 16px}h3{font-size:14px;margin:20px 0 8px}.sub,p.help{color:var(--muted);font-size:13px}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:14px}.card{background:var(--card);border:1px solid var(--line);border-radius:16px;padding:22px;margin-top:16px}.hero{grid-column:span 2;position:relative}.hero .value{font-size:clamp(52px,9vw,90px);line-height:1.1;font-weight:600;letter-spacing:-5px;margin:12px 0}.unit{font-size:18px;letter-spacing:0;color:var(--muted);margin-left:10px}.label{color:var(--muted);font-size:12px;text-transform:uppercase;letter-spacing:1px}.metric{font-size:30px;margin:8px 0}.pill{display:inline-block;border:1px solid var(--line);border-radius:30px;font-size:12px;padding:5px 10px;margin-right:6px}.warning{color:var(--warn)}.row{display:flex;align-items:center;justify-content:space-between;gap:12px;flex-wrap:wrap}.actions{display:flex;gap:10px;flex-wrap:wrap;margin-top:16px}canvas{width:100%;height:240px;display:block;margin-top:20px}.two{display:grid;grid-template-columns:1fr 1fr;gap:16px}.formgrid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:18px}label{display:flex;flex-direction:column;gap:7px;color:var(--muted);font-size:13px}input,select{width:100%;font:inherit;color:var(--text);background:#101919;border:1px solid var(--line);border-radius:8px;padding:10px;min-height:42px}input[type=checkbox]{width:22px;height:22px;min-height:0;accent-color:var(--accent)}input[type=color]{padding:3px;max-width:100px}input[type=range]{padding:0;accent-color:var(--accent)}.check{flex-direction:row;align-items:center;justify-content:space-between;border-bottom:1px solid var(--line);padding:8px 0}.wide{grid-column:1/-1}.settings-note{border-left:3px solid var(--accent);padding:8px 14px;margin:18px 0;color:var(--muted)}.page[hidden]{display:none}table{width:100%;border-collapse:collapse;font-size:13px}td,th{padding:9px 6px;border-bottom:1px solid var(--line);text-align:left}th{color:var(--muted);font-weight:500}pre{white-space:pre-wrap;font:12px/1.7 ui-monospace,monospace;word-break:break-word;color:var(--muted)}code{font-size:13px}.pin{font-size:32px;letter-spacing:3px;font-variant-numeric:tabular-nums}#toast{position:fixed;bottom:24px;left:50%;transform:translateX(-50%);max-width:90%;padding:14px 20px;background:var(--accent);color:#15200f;border-radius:12px;box-shadow:0 10px 40px #0008;z-index:5}#toast[hidden]{display:none}.error{color:var(--warn)}.spaced{margin-top:24px}footer{color:var(--muted);font-size:12px;margin-top:30px}progress{width:100%;height:12px;accent-color:var(--accent)}button:focus-visible,input:focus-visible,select:focus-visible,a:focus-visible{outline:2px solid var(--accent);outline-offset:3px}@media(max-width:650px){main{padding:18px 16px 60px}.grid,.two{grid-template-columns:1fr}.hero{grid-column:auto}.formgrid{grid-template-columns:1fr}.card{padding:18px}.brand small{display:block;margin:2px 0 0}h1{font-size:26px}canvas{height:210px}.wide{grid-column:auto}}
</style></head><body><main>
<header><div class="brand">CO₂ Monitor <small>Повітря у твоєму просторі</small></div><span class="status" id="connection">Підключення…</span></header>
<nav aria-label="Розділи"><button data-tab="overview" aria-selected="true">Огляд</button><button data-tab="settings">Налаштування</button><button data-tab="integrations">Підключення</button><button data-tab="service">Калібрування</button><button data-tab="system">Система</button></nav>
<section class="page" id="overview"><h1>Знати, коли провітрити.</h1><p class="sub">Поточні покази, зміни протягом дня та результати провітрювання.</p>
<div class="grid"><div class="card hero"><div class="row"><span class="label">Вуглекислий газ</span><span id="mode" class="pill">—</span></div><div class="value"><span id="co2">—</span><span class="unit">ppm</span></div><div id="advice">Очікуємо вимірювання</div><div class="sub spaced" id="trend">Для тенденції потрібно щонайменше 3 хвилини даних</div></div><div class="card"><div class="label">Температура</div><div class="metric" id="temperature">—</div><div class="label spaced">Вологість</div><div class="metric" id="humidity">—</div></div></div>
<div class="card"><div class="row"><h2>Як змінювалося CO₂</h2><div><button id="hour" class="primary">Година</button> <button id="day">Доба</button></div></div><canvas id="chart" role="img" aria-label="Графік CO₂ за вибраний період"></canvas><div class="row sub"><span id="chart-info">Історія накопичується щохвилини</span><a class="button" href="/api/history.csv">Завантажити CSV</a></div></div>
<div class="two"><div class="card"><h2 id="night-title">Підсумок ночі</h2><p id="night-summary" class="sub">Поки немає даних.</p></div><div class="card"><h2>Провітрювання</h2><p id="vent-summary" class="sub">Почни сесію перед відкриванням вікна.</p><div class="actions"><button class="primary" id="vent-start">Почати</button><button id="vent-stop">Завершити</button></div></div></div>
<div class="card"><div class="row"><h2>Пристрій поруч</h2><button id="led-toggle">Підсвітка</button></div><div class="actions"><button data-screen="0">Основний екран</button><button data-screen="1">Графік години</button><button data-screen="2">Графік доби</button><button data-screen="3">Ніч / провітрювання</button><button data-screen="4">Підключення</button></div><p class="help">Вибрана сторінка з’явиться на OLED на 60 секунд. Потім повернеться основний екран.</p></div></section>
<section class="page" id="settings" hidden><h1>Налаштуй під себе.</h1><p class="sub">Повзунки показують яскравість на пристрої на 10 секунд; кнопка збереження застосовує її постійно. Кожна група зберігається окремо. Параметри залишаються після вимкнення живлення.</p><div id="settings-forms"></div><div class="card"><h2>Кольори CO₂</h2><div id="palette" class="formgrid"></div><div class="actions"><button id="save-palette" class="primary">Зберегти палітру</button></div><p class="help">Попередній перегляд триває 10 секунд і використовує поточну яскравість. Увімкни підсвітку перед перевіркою.</p></div></section>
<section class="page" id="integrations" hidden><h1>Телефон, дім і монітор.</h1><p class="sub">Мережеві зміни перезапускають пристрій. За відсутності домашнього Wi-Fi мережа налаштування повернеться автоматично.</p><div id="network-forms"></div><div class="card"><div class="row"><h2>Apple Home</h2><span id="hk-state" class="pill">—</span></div><p class="help">Підключи монітор і iPhone до домашньої мережі. У «Домі»: додати аксесуар → інші варіанти → CO₂ Monitor → ввести код. Для цієї інтеграції HomeSpan потрібен домашній центр HomePod або Apple TV.</p><div class="pin" id="pairing-code">—</div><div class="actions"><button id="hk-show">Показати код на OLED</button><button id="hk-reset">Скинути сполучення</button></div><p class="help">Це саморобний аксесуар без сертифікації Apple. За помилки сенсора HomeKit передає стан несправності; деякі версії «Дому» можуть залишати останнє числове значення. Детальну свіжість видно тут.</p></div></section>
<section class="page" id="service" hidden><h1>Точність починається тут.</h1><p class="sub">Проводь процедури окремо, у стабільних умовах. Результат кожної перевірки з’явиться на цій сторінці.</p>
<div class="card"><h2>Датчик освітлення</h2><p class="help">1. Закрий TEMT6000 та запиши темну точку. 2. Відкрий і встанови звичайне денне освітлення. 3. Запиши світлу точку й збережи. Кожне вимірювання триває 5 секунд.</p><div class="actions"><button data-action="cal_dark">1 · Темна точка</button><button data-action="cal_bright">2 · Світла точка</button><button data-action="cal_apply" class="primary">3 · Зберегти</button></div><p id="cal-result">—</p><p class="sub" id="cal-values">—</p></div>
<div class="card"><h2>Чи бачить датчик власну підсвітку?</h2><p class="help">Не змінюй освітлення кімнати протягом 21 секунди. Пристрій перевірить вимкнену індикацію, R/G/B, OLED, їх поєднання та повторно вимкнену індикацію. Під час тесту LED може світитися з денною яскравістю.</p><button id="optical-start">Почати перевірку</button><p id="optical-state" class="sub">Ще не виконано</p><table><thead><tr><th>Стан</th><th>ADC</th><th>Різниця з темною індикацією</th></tr></thead><tbody id="optical-results"></tbody></table></div>
<div id="sensor-form"></div>
<div class="card"><h2>Обслуговування SCD40</h2><p id="sensor-info" class="sub">—</p><button data-action="self_test">Самоперевірка сенсора</button><p class="help">Самоперевірка зупиняє вимірювання приблизно на 10 секунд, після чого вони відновляться.</p><h3>Примусове калібрування CO₂</h3><p class="help">Потрібен відомий еталон, стабільне середовище та щонайменше 3 хвилини безперервної роботи в поточному режимі. Не підставляй довільне значення «свіжого повітря».</p><label>Еталонна концентрація, ppm<input id="frc-reference" type="number" min="400" max="2000" placeholder="400–2000"></label><div class="actions"><button id="frc-start">Калібрувати за еталоном</button></div><p id="sensor-result">—</p></div></section>
<section class="page" id="system" hidden><h1>Система й обслуговування.</h1><div class="card"><h2>Діагностика</h2><pre id="diagnostics">—</pre></div><div class="card"><h2>Резервна копія налаштувань</h2><p class="help">Копія не містить паролів Wi-Fi, MQTT та сполучення Apple Home. Відновлення перевіряє всі значення перед застосуванням.</p><div class="actions"><a class="button" href="/api/backup">Завантажити JSON</a><button id="restore">Відновити з JSON</button><input id="restore-file" type="file" accept=".json,application/json" hidden></div></div>
<div class="card"><h2>Оновлення прошивки</h2><p class="help">Обери application .bin саме для своєї плати й цього проєкту (не merged/bootloader). Не вимикай живлення. Нова версія має підтвердити працездатність; якщо цього не станеться, завантажувач може повернути попередню версію.</p><input id="firmware-file" type="file" accept=".bin"><div class="actions"><button class="primary" id="firmware-upload">Завантажити прошивку</button></div><p id="upload-state" class="sub"></p><progress id="upload-progress" max="100" value="0" hidden></progress></div>
<div class="card"><h2>Архів і відновлення доступу</h2><p class="help">Архів зберігається пакетами раз на 15 хвилин за ввімкненого архівування й синхронізованого часу. Первинне форматування стирає лише архів, не налаштування.</p><div class="actions"><button id="format">Форматувати архів</button><button id="wifi-reset">Скинути Wi-Fi</button><button data-action="reboot">Перезапустити</button></div><p class="help">Через USB/Serial, 115200 бод: <code>access</code> показує пароль і відкриває мережу налаштування; <code>wifi SSID|PASSWORD</code> змінює мережу; <code>wifi reset</code> скидає її.</p></div><div class="card"><h2>Події цього запуску</h2><pre id="events">—</pre></div></section>
<footer>CO₂ Monitor <span id="version"></span> · Усі дані обробляються локально. Мережевий час використовує NTP; MQTT працює лише за твоїм налаштуванням.</footer>
</main><div id="toast" role="status" hidden></div>
<script>
'use strict';
const $=id=>document.getElementById(id);
let state=null,settings=null,records=[],windowSize=60,uploading=false,refreshing=false,historyAt=0,toastTimer;
const field=(key,label,type='number',min,max,step=1)=>({key,label,type,min,max,step});
const groups=[
 {target:'settings-forms',title:'Яскравість і ніч',fields:[field('led_enabled','Підсвітка LED','checkbox'),field('smooth_light','Плавна адаптація','checkbox'),field('led_day','LED удень', 'range',0,255),field('led_night','LED уночі','range',0,255),field('oled_day','OLED удень','range',0,255),field('oled_night','OLED уночі','range',0,255),field('oled_sleep','Гасити OLED уночі','checkbox'),field('gesture','Жест прикривання / відкривання','checkbox'),field('gamma','Крива яскравості', 'number',1,3,.1)],note:'Жести експериментальні й працюють лише при достатньому денному освітленні. Вони змінюють сторінку, але ніколи не скидають налаштування.'},
 {target:'settings-forms',title:'Пороги й аналітика',fields:[field('alarm_ppm','Поріг попередження, ppm','number',400,10000),field('alarm_seconds','Безперервне перевищення, секунд','number',5,1800),field('co2_hysteresis','Гістерезис кольорів, ppm','number',0,100),field('vent_target','Ціль провітрювання, ppm','number',400,2000),field('alarm_pulse','Повільний імпульс попередження вдень','checkbox'),field('eco','Економний режим: вимір раз на 30 с','checkbox'),field('archive','Зберігати історію у flash','checkbox')],note:'Пороги — твої налаштування індикації. Економний режим змінює період вимірювання та може потребувати іншого температурного offset.'},
 {target:'settings-forms',title:'Точні параметри освітлення',fields:[field('dark_adc','Темна точка ADC','number',0,4094),field('bright_adc','Світла точка ADC','number',1,4095),field('light_low','Поріг ночі','number',.01,.95,.01),field('light_high','Поріг дня','number',.05,.99,.01)],note:'Зручніше спочатку скористатися майстром у розділі калібрування.'},
 {target:'network-forms',title:'Домашній Wi-Fi',fields:[field('wifi_enabled','Wi-Fi увімкнено','checkbox'),field('wifi_tx_dbm','Макс. потужність Wi-Fi, dBm','number',8.5,19.5,0.5),field('ssid','Назва мережі (SSID)','text'),field('wifi_password','Пароль Wi-Fi','password'),field('homekit','Apple Home / HomeKit','checkbox'),field('timezone','Часовий пояс POSIX','text')],note:'Якщо Wi-Fi вимкнено, мережа налаштування доступна протягом першої хвилини після запуску та доки до неї підключений телефон. Порожнє поле пароля зберігає поточний пароль. Для переходу на відкриту мережу спочатку скинь Wi-Fi. Після збереження відкрий нову IP-адресу з OLED.'},
 {target:'network-forms',title:'MQTT / Home Assistant',fields:[field('mqtt_enabled','Передавати вимірювання','checkbox'),field('mqtt_host','Адреса локального брокера','text'),field('mqtt_port','Порт','number',1,65535),field('mqtt_user','Користувач','text'),field('mqtt_password','Пароль','password')],note:'Home Assistant discovery додає CO₂, температуру й вологість. Використовується звичайний MQTT без TLS: призначено для довіреної локальної мережі. Порожнє поле зберігає пароль.'},
 {target:'sensor-form',title:'Компенсація SCD40',fields:[field('sensor_override','Застосовувати ці параметри до сенсора','checkbox'),field('temperature_offset','Температурний offset, °C','number',0,20,.1),field('altitude','Висота над рівнем моря, м','number',0,3000),field('pressure_pa','Поточний тиск, Па (0 — за висотою)','number',0,120000),field('asc','Автокалібрування ASC','checkbox')],note:'ASC потребує регулярного контакту зі свіжим повітрям. Offset вимірюють у зібраному корпусі після теплової стабілізації. Тиск 70000–120000 Па має бути місцевим, не приведеним до рівня моря. Параметри зберігаються на ESP32 й застосовуються при запуску без зайвих записів EEPROM сенсора.'}
];
function toast(message,error=false){$('toast').textContent=message;$('toast').style.background=error?'#ffc37a':'#c3ef98';$('toast').hidden=false;clearTimeout(toastTimer);toastTimer=setTimeout(()=>$('toast').hidden=true,6500);}
async function api(path,body){
 const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),7000);
 try{const options={cache:'no-store',signal:controller.signal};
 if(body!==undefined){options.method='POST';options.headers={'Content-Type':'application/json','X-CO2-Request':'1'};if(path==='/api/settings'&&settings)options.headers['X-CO2-Revision']=String(settings._revision);options.body=JSON.stringify(body);}
 const response=await fetch(path,options);let result;
 try{result=await response.json();}catch{throw Error('Пристрій недоступний або відповідь пошкоджена');}
 if(!response.ok)throw Error(result.error||`HTTP ${response.status}`);
 if(path==='/api/settings'&&body!==undefined&&settings)settings._revision=result.revision;
 return result;}finally{clearTimeout(timer);}
}
async function action(name,extra={}){try{await api('/api/action',{action:name,...extra});toast(name==='cal_apply'?'Калібрування збережено. Онови сторінку перед редагуванням налаштувань.':'Команду прийнято');await refresh();}catch(e){toast(e.message,true);}}
function showPage(name){document.querySelectorAll('.page').forEach(p=>p.hidden=p.id!==name);document.querySelectorAll('[data-tab]').forEach(b=>b.setAttribute('aria-selected',b.dataset.tab===name));if(name==='overview')drawChart();}
document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>showPage(b.dataset.tab));
function renderForms(){for(const group of groups){const form=document.createElement('form');form.className='card';const title=document.createElement('h2');title.textContent=group.title;form.append(title);const grid=document.createElement('div');grid.className='formgrid';for(const f of group.fields){const label=document.createElement('label');if(f.type==='checkbox')label.className='check';const caption=document.createElement('span');caption.textContent=f.label;label.append(caption);const input=document.createElement('input');input.name=f.key;input.type=f.type;input.id='setting-'+f.key;if(f.min!==undefined)input.min=f.min;if(f.max!==undefined)input.max=f.max;input.step=f.step;input.autocomplete=f.type==='password'?'new-password':'off';if(f.type==='checkbox')input.checked=settings[f.key];else input.value=f.type==='password'?'':typeof settings[f.key]==='number'?Number(settings[f.key].toFixed(3)):settings[f.key];if(f.type==='password')input.placeholder='Без змін';label.append(input);if(f.type==='range'){const output=document.createElement('output');output.textContent=input.value;let previewTimer;input.oninput=()=>{output.textContent=input.value;clearTimeout(previewTimer);previewTimer=setTimeout(()=>api('/api/action',{action:'brightness_preview',target:f.key.startsWith('led')?'led':'oled',value:Number(input.value)}).catch(e=>toast(e.message,true)),180);};label.append(output);}grid.append(label);}form.append(grid);const note=document.createElement('p');note.className='help';note.textContent=group.note;form.append(note);const button=document.createElement('button');button.className='primary';button.type='submit';button.textContent='Зберегти';form.append(button);form.onsubmit=async event=>{event.preventDefault();const patch={};for(const f of group.fields){const input=form.elements[f.key];if(f.type==='password'&&!input.value)continue;patch[f.key]=f.type==='checkbox'?input.checked:['number','range'].includes(f.type)?Number(input.value):input.value;}button.disabled=true;try{const result=await api('/api/settings',patch);Object.assign(settings,patch);delete settings.wifi_password;delete settings.mqtt_password;for(const f of group.fields)if(f.type==='password')form.elements[f.key].value='';toast(result.reboot?'Збережено. Пристрій перезапускається; адресу дивись на OLED.':'Збережено');}catch(e){toast(e.message,true);}finally{button.disabled=false;}};$(group.target).append(form);}renderPalette();}
function renderPalette(){for(let i=0;i<5;i++){const label=document.createElement('label');label.textContent=`Колір ${i+1}`;const input=document.createElement('input');input.type='color';input.id=`color-${i}`;input.value='#'+settings.colors[i].map(v=>v.toString(16).padStart(2,'0')).join('');label.append(input);const preview=document.createElement('button');preview.textContent='Переглянути збережений';preview.type='button';preview.onclick=()=>action('preview',{color:i});label.append(preview);$('palette').append(label);}for(let i=0;i<4;i++){const label=document.createElement('label');label.textContent=`Межа ${i+1}, ppm`;const input=document.createElement('input');input.type='number';input.min=200;input.max=10000;input.id=`threshold-${i}`;input.value=settings.thresholds[i];label.append(input);$('palette').append(label);}for(let i=0;i<3;i++){const label=document.createElement('label');label.textContent=`Підсилення ${['R','G','B'][i]}`;const input=document.createElement('input');input.type='number';input.min=0;input.max=255;input.id=`gain-${i}`;input.value=settings.gains[i];label.append(input);$('palette').append(label);}}
$('save-palette').onclick=async()=>{const colors=Array.from({length:5},(_,i)=>{const s=$(`color-${i}`).value;return [1,3,5].map(p=>parseInt(s.slice(p,p+2),16));});const thresholds=Array.from({length:4},(_,i)=>Number($(`threshold-${i}`).value));const gains=Array.from({length:3},(_,i)=>Number($(`gain-${i}`).value));try{await api('/api/settings',{colors,thresholds,gains});Object.assign(settings,{colors,thresholds,gains});toast('Палітру збережено');}catch(e){toast(e.message,true);}};
function updateStatus(s){state=s;$('connection').textContent=s.wifi_connected?'Домашній Wi-Fi · на зв’язку':'Локальне підключення';$('co2').textContent=s.co2??'—';$('temperature').textContent=s.temperature==null?'—':s.temperature.toFixed(1)+' °C';$('humidity').textContent=s.humidity==null?'—':Math.round(s.humidity)+' %';$('mode').textContent=s.night?'Ніч':'День';$('advice').textContent=!s.fresh?'Немає свіжих даних сенсора':s.suspicious?'Незвично низьке CO₂: перевір калібрування':s.alarm?'Тривале перевищення твого порога — провітри':s.co2>2000?'Понад діапазон заявленої точності SCD40':'Вимірювання оновлюються';$('advice').classList.toggle('warning',!s.fresh||s.alarm||s.suspicious);$('trend').textContent=s.slope==null?'Для тенденції потрібно щонайменше 3 хвилини даних':`${s.slope>10?'↗ Зростає':s.slope< -10?'↘ Знижується':'→ Стабільно'} · ${s.slope>0?'+':''}${Math.round(s.slope)} ppm/хв`;
const n=s.night_stats;$('night-title').textContent=s.night_active?'Цієї ночі':'Остання ніч';$('night-summary').textContent=n.seconds?`Середнє ${n.average} ppm · максимум ${n.peak} ppm. Вище порога: ${Math.round(n.above_seconds/60)} хв із ${Math.round(n.seconds/60)} хв спостережень.`:'Дані з’являться після роботи в нічному режимі.';const v=s.vent;$('vent-summary').textContent=v.active||v.done?`${v.active?'Триває':'Завершено'}: ${v.start} → ${v.current} ppm за ${Math.floor(v.seconds/60)} хв${!s.fresh&&v.active?' · очікуємо сенсор':''}.`:'Почни сесію перед відкриванням вікна.';$('vent-start').disabled=!s.fresh||v.active;$('vent-stop').disabled=!v.active;$('led-toggle').textContent=s.led_enabled?'Вимкнути LED':'Увімкнути LED';$('hk-state').textContent=!s.homekit_enabled?'Вимкнено / Wi-Fi не задано':s.homekit_paired?'Сполучено':'Готовий до сполучення';$('pairing-code').textContent=s.pairing_code.replace(/^(...)(..)(...)$/,'$1-$2-$3');$('cal-result').textContent=s.calibration.result;$('cal-values').textContent=`ADC зараз ${s.adc} · фільтр ${s.filtered_adc.toFixed(1)}. Темна: ${s.calibration.dark.toFixed(1)} ± ${s.calibration.dark_noise.toFixed(1)}; світла: ${s.calibration.bright.toFixed(1)} ± ${s.calibration.bright_noise.toFixed(1)}.`;$('sensor-result').textContent=s.sensor_result;$('sensor-info').textContent=`Серійний номер ${s.sensor_serial} · offset ${s.sensor_offset.toFixed(1)} °C · висота ${s.sensor_altitude} м · ASC ${s.sensor_asc?'увімкнено':'вимкнено'}`;const o=s.optical;$('optical-state').textContent=o.active?`Етап ${o.stage+1} із 7…`:o.stage>=7?'Завершено. Порівняй першу й останню темні точки: їх різниця може означати зміну зовнішнього світла.':'Ще не виконано';$('optical-results').replaceChildren();['Вимкнено','Червоний','Зелений','Синій','OLED','Разом','Знову вимкнено'].forEach((name,i)=>{const tr=document.createElement('tr');[name,o.levels[i].toFixed(1),(o.levels[i]-o.levels[0]).toFixed(1)].forEach(t=>{const td=document.createElement('td');td.textContent=t;tr.append(td);});$('optical-results').append(tr);});$('diagnostics').textContent=[`Версія: ${s.version} · ${s.device_id}`,`Чип: ${s.chip} · flash ${(s.flash_bytes/1048576).toFixed(1)} MiB`,`Час роботи: ${Math.floor(s.uptime_seconds/60)} хв · NTP: ${s.epoch?'синхронізовано':'час не встановлено'}`,`IP: ${s.ip} · MQTT: ${s.mqtt_connected?'підключений':'відключений'}`,`Вільна RAM: ${s.heap_free} B · мінімум: ${s.heap_min} B`,`Сенсор: помилок ${s.sensor_errors}, відновлень ${s.sensor_restarts}`,`Файлова система: ${s.filesystem?'готова':'не змонтована'} · архів ${s.archive_enabled?'увімкнений':'вимкнений'}`,`Причина перезапуску: ${s.reset_reason}`,s.last_network_error].filter(Boolean).join('\n');$('events').textContent=s.events.map(e=>`[${e.seconds} с] ${e.message}`).join('\n')||'Подій ще немає';$('version').textContent=s.version;}
async function refresh(){if(uploading||refreshing)return;refreshing=true;try{const s=await api('/api/status');updateStatus(s);if(Date.now()-historyAt>30000){records=await api('/api/history');historyAt=Date.now();drawChart();}}catch(e){$('connection').textContent='Немає зв’язку · повторюємо';$('co2').textContent='—';$('temperature').textContent='—';$('humidity').textContent='—';$('advice').textContent='З’єднання втрачено. Перевір Wi-Fi та адресу на OLED.';$('trend').textContent='';$('vent-start').disabled=true;}finally{refreshing=false;}}
function drawChart(){const canvas=$('chart');if(!canvas.clientWidth)return;const dpr=window.devicePixelRatio||1,w=canvas.clientWidth,h=canvas.clientHeight;canvas.width=w*dpr;canvas.height=h*dpr;const c=canvas.getContext('2d');c.scale(dpr,dpr);const data=records.slice(-windowSize),valid=data.filter(r=>r[2]!=null);c.font='11px system-ui';c.fillStyle='#9cb0a8';if(!valid.length){c.fillText('Перша точка з’явиться приблизно через хвилину.',20,h/2);$('chart-info').textContent='Історія накопичується щохвилини';return;}const min=Math.max(0,Math.floor(Math.min(...valid.map(r=>r[2]))/100)*100-100),max=Math.max(min+200,Math.ceil(Math.max(...valid.map(r=>r[2]))/100)*100+100),left=44,right=w-12,top=10,bottom=h-30;for(let i=0;i<=4;i++){const y=top+(bottom-top)*i/4;c.strokeStyle='#30413f';c.beginPath();c.moveTo(left,y);c.lineTo(right,y);c.stroke();c.fillStyle='#9cb0a8';c.fillText(Math.round(max-(max-min)*i/4),0,y+4);}c.strokeStyle='#c3ef98';c.lineWidth=2;c.beginPath();let drawing=false;data.forEach((r,i)=>{if(r[2]==null){drawing=false;return;}const x=left+(right-left)*i/Math.max(1,data.length-1),y=bottom-(r[2]-min)/(max-min)*(bottom-top);if(drawing)c.lineTo(x,y);else c.moveTo(x,y);drawing=true;});c.stroke();if(data.length===1){c.fillStyle='#c3ef98';c.beginPath();c.arc(left,bottom-(data[0][2]-min)/(max-min)*(bottom-top),3,0,Math.PI*2);c.fill();}const label=r=>r[0]?new Date(r[0]*1000).toLocaleTimeString('uk-UA',{hour:'2-digit',minute:'2-digit'}):`${r[1]} хв від старту`;c.fillStyle='#9cb0a8';c.fillText(label(data[0]),left,h-8);const end=label(data[data.length-1]);c.fillText(end,right-c.measureText(end).width,h-8);$('chart-info').textContent=`${valid.length} хв із даними · мін ${Math.min(...valid.map(r=>r[2]))} / макс ${Math.max(...valid.map(r=>r[2]))} ppm · прогалини не з’єднуються`;}
$('hour').onclick=()=>{windowSize=60;$('hour').className='primary';$('day').className='';drawChart();};$('day').onclick=()=>{windowSize=1440;$('day').className='primary';$('hour').className='';drawChart();};window.addEventListener('resize',drawChart);
document.querySelectorAll('[data-screen]').forEach(b=>b.onclick=()=>action('screen',{page:Number(b.dataset.screen)}));document.querySelectorAll('[data-action]').forEach(b=>b.onclick=()=>action(b.dataset.action));$('vent-start').onclick=()=>action('vent_start');$('vent-stop').onclick=()=>action('vent_stop');$('led-toggle').onclick=async()=>{if(!state)return;try{await api('/api/settings',{led_enabled:!state.led_enabled});await refresh();}catch(e){toast(e.message,true);}};
$('optical-start').onclick=()=>{if(confirm('Протягом 21 секунди індикація змінюватиметься, зокрема з денною яскравістю. Почати?'))action('optical_test');};$('hk-show').onclick=()=>action('homekit_pair');$('hk-reset').onclick=()=>{if(confirm('Видалити сполучення Apple Home? Потім потрібно видалити старий аксесуар у «Домі» й додати його знову.'))action('homekit_reset',{confirm:'UNPAIR'});};$('frc-start').onclick=()=>{const reference=Number($('frc-reference').value);if(reference<400||reference>2000){toast('Вкажи відомий еталон 400–2000 ppm',true);return;}if(confirm(`Виконати калібрування за еталоном ${reference} ppm? Сенсор має бути у стабільному еталонному середовищі.`))action('recalibrate',{reference,confirm:'REFERENCE'});};$('format').onclick=()=>{if(confirm('Стерти весь архів у flash та створити файлову систему?'))action('archive_format',{confirm:'FORMAT ARCHIVE'});};$('wifi-reset').onclick=()=>{if(confirm('Скинути домашній Wi-Fi й повернути пристрій у мережу налаштування?'))action('wifi_reset',{confirm:'RESET WIFI'});};
$('restore').onclick=()=>$('restore-file').click();$('restore-file').onchange=async()=>{const file=$('restore-file').files[0];if(!file)return;try{if(file.size>8192)throw Error('Файл налаштувань завеликий');const data=JSON.parse(await file.text());if(!confirm('Замінити параметри значеннями з цієї копії?'))return;const result=await api('/api/settings',data);toast(result.reboot?'Відновлено; пристрій перезапускається':'Відновлено. Онови сторінку для перегляду параметрів.');}catch(e){toast(e.message,true);}finally{$('restore-file').value='';}};
$('firmware-upload').onclick=()=>{const file=$('firmware-file').files[0];if(!file){toast('Обери application .bin',true);return;}if(!confirm('Завантажити прошивку й перезапустити пристрій?'))return;uploading=true;$('firmware-upload').disabled=true;$('upload-progress').hidden=false;const xhr=new XMLHttpRequest();xhr.open('POST','/api/update');xhr.setRequestHeader('X-CO2-Request','1');xhr.upload.onprogress=e=>{if(e.lengthComputable){$('upload-progress').value=100*e.loaded/e.total;$('upload-state').textContent=`Передавання: ${Math.round(100*e.loaded/e.total)}%`;}};xhr.onload=()=>{uploading=false;$('firmware-upload').disabled=false;let data={};try{data=JSON.parse(xhr.responseText);}catch{}$('upload-state').textContent=xhr.status===200?'Образ прийнято. Пристрій перезапускається; перевірка працездатності триває до 3 хвилин.':data.error||'Оновлення відхилено';};xhr.onerror=()=>{uploading=false;$('firmware-upload').disabled=false;$('upload-state').textContent='З’єднання втрачено. Перевір пристрій перед повторною спробою.';};const body=new FormData();body.append('firmware',file);xhr.send(body);};
(async()=>{try{settings=await api('/api/settings');renderForms();}catch(e){toast(e.message,true);}await refresh();setInterval(refresh,3000);})();
</script></body></html>)CO2HTML";
